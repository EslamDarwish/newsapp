import styled from 'styled-components/native';
import {View} from '../../components/atoms';
import {spacing} from '../../constants';

export const NewsPageContainer = styled(View)`
  flex: 1;
  padding-horizontal: ${spacing.md}px;
`;

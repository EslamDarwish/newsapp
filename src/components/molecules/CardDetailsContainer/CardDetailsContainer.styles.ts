import styled from 'styled-components/native';
import {View} from '../../atoms';
import {colors, radius, spacing} from '../../../constants';

export const StyledCardDetailsContainer = styled(View)`
  margin-bottom: ${spacing.md}px;
`;
